<?php include VIEW . 'user/header.phtml';?>

    <div id="main-first-row-container">
        <div id="page-title">
            <h1>Activity</h1>
        </div>
    </div>

    <div id="main-second-row-container">
    <div class="container">
        <ul>
            <?php
            if(!empty($this->view_data["logs"]))
            {
                foreach ($this->view_data["logs"] as $log)
                {
                    echo "<li>";

                    $date = null;
                    $description = null;

                    foreach($log as $key => $value)
                    {
                        if(strpos($key, "LogCreatedAt") !== false)
                        {
                            $date = " <u>" . $value . "</u> ";
                        }
                        if(strpos($key, "LogDescription") !== false)
                        {
                            $description = " $value";
                        }
                    }

                    echo $date . $description;

                    echo "</li>";
                }
            }
            else
            {
                echo "<li> <u>There are no activity logs! </u></li>";
            }
            ?>
        </ul>
    </div>
    </div>

<?php include VIEW . 'user/footer.phtml';?>